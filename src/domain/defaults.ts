// Reference model used by ModelPicker to show the "default" badge and to
// compare pricing for the expensive-confirm modal. Picked to match the
// Council Lite plan chairman so the badge appears on a known cheap model.
export const DEFAULT_MODEL = 'google/gemini-2.5-flash-lite'
